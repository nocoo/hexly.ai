# Accepted precedent: Pi Agent Policy, campaign 1.0.3

This is the material-tool precedent, not a botanical template for animals or
birds. The owner approved its generated surfaces and final local page on
2026-09-14, then authorized publication. The official identity stays at 1.0.0.

## Evidence and exact source

Paths below are relative to the repository root:

- `artwork/brands/pi-agent-policy/texture-studies/2026-09-14-flare-01/brief.md`
  records the product analogy and actual generation route.
- The same study's `light/prompt.txt` and `dark/prompt.txt` are the exact model
  prompts. Their request/response and raw-review JSON retain actual records.
- `artwork/brands/pi-agent-policy/v1.0.3/recipe.json` and `refine.ts` record
  assembly; `verification.json` lists preserved/changed files and raw hashes.
- `artwork/brands/pi-agent-policy/v1.0.3/inspection/browser.json` records actual
  local desktop/mobile/theme/contrast checks.
- `public/brands/pi-agent-policy/v1.0.3/manifest.json`, `provenance.json`,
  `guide.md` and `license.txt` describe the immutable downloadable kit.
- `docs/26-pi-agent-policy-onboarding.md` and R2 publication receipts record
  the later publication state. Local fields in frozen source receipts describe
  their creation time; do not rewrite history to mark them published.

The helper was the real Workflow `agi-image-generation/scripts/generate.py`,
revision `cd22362da862248bfd6fe3028630f532a135b322`, called through Azure OpenAI
`/openai/v1/images/generations`. Two successful Flare/high calls returned one
native 1024² PNG per theme; no reference image was submitted. The archive wrapper
records metadata without modifying that helper. Inspect its hardcoded batch
paths before adapting it for a different project; never rerun the accepted batch.

## Why the surface works

Pi Agent Policy declares tool rules, intercepts actions and permits one shared
repair allowance per original user input. Its approved Logo is a reset instrument.
The surface is a precision service mat: pressed locating squares convey rules,
recessed contacts convey interception, and one terracotta return conveys bounded
repair. The matte tooth and shallow groove lips make it a tangible working
surface rather than the earlier thin, barely visible SVG circuit drawing.

Prompt palette targets came from the site, not from recoloring the Logo:

| Theme | Targets |
| --- | --- |
| Light | Paper `#f0f0e9`, ink `#30372e`, small terracotta `#bf5c3c` accent |
| Dark | Page `#1e2824`, surface `#27332c`, ink `#e6e9dc`, small `#e79670` accent |

Actual generated pixels vary around those targets. Each output was reviewed
independently; the dark one is not a CSS inversion of the light one.

## Accepted integration

The prompts requested seamless edges and 10% motif clearance, but the returned
grooves touch the top edge. Inspection therefore selected `display: "single"`.
The complete square is displayed with `contain` and `no-repeat`, without crop,
vector tracing, synthesized edge repair or a false seamless claim.

| Surface | Measured treatment |
| --- | --- |
| Texture specimens | Full square at native aspect; two columns on desktop, stacked on mobile |
| Card, intro, Hero caption | Right-aligned square pseudo-element; contain/no-repeat; opacity 0.45 |
| Edge transition | CSS mask from transparent at the left edge to opaque at 24%; raw PNG is untouched |
| Dark text on the material | Existing Hexly light-surface `#f8f8f2`, fully opaque |
| Text contrast | Minimum 4.9428:1 light and 4.8956:1 dark across tested composite pixels |
| Screens | Desktop 1440px, mobile 390px and narrow 320px, both themes |
| Delivery | Native PNG download; same-size WebP, quality 90 / effort 6 |

The full Logo, icons, wordmarks, fonts and pre-existing Hero images retain their
original bytes. Only the surrounding caption gets the new surface; the Hero
image is not silently recomposed. The kit contains 67 files plus its manifest.

For another tool, reuse the hierarchy and validation method, not these exact
grooves. For an animal or bird, first switch to the habitat-related leaves and
restrained flowers required by the parent skill, then find its own geometry,
material detail, motif scale and contrast treatment.
