diff --git a/templates/startDevWorker/ProxyWorker.ts b/templates/startDevWorker/ProxyWorker.ts
index 59b507d72fea2aed32dee4fdcc3fa5c04418cda1..af3df7a0e4264192add9151fa530249156cebf31 100644
--- a/templates/startDevWorker/ProxyWorker.ts
+++ b/templates/startDevWorker/ProxyWorker.ts
@@ -165,6 +165,22 @@ export class ProxyWorker implements DurableObject {
 
 			// explicitly NOT await-ing this promise, we are in a loop and want to process the whole queue quickly + synchronously
 			void fetch(userWorkerUrl, new Request(request, { headers }))
+				.catch((error: Error) => {
+					// One retry for a lost read connection; never replay writes or upgrades.
+					if (
+						(request.method === "GET" || request.method === "HEAD") &&
+						!request.signal.aborted &&
+						!request.headers.has("Upgrade") &&
+						error.message === "Network connection lost." &&
+						isSameUserWorkerOrigin(userWorkerUrl, this.proxyData?.userWorkerUrl)
+					) {
+						return fetch(userWorkerUrl, new Request(request, { headers }));
+					}
+					throw error;
+				})
+				.catch((error: Error) => {
+					throw new UserWorkerFetchError(error);
+				})
 				.then(async (res) => {
 					res = new Response(res.body, res);
 					rewriteUrlRelatedHeaders(res.headers, innerUrl, outerUrl);
@@ -197,6 +213,25 @@ export class ProxyWorker implements DurableObject {
 					if (
 						isSameUserWorkerOrigin(userWorkerUrl, this.proxyData?.userWorkerUrl)
 					) {
+						if (error instanceof UserWorkerFetchError) {
+							void sendMessageToProxyController(this.env, {
+								type: "debug-log",
+								args: [
+									"Could not proxy request to the UserWorker:",
+									request.method,
+									request.url,
+									error.message,
+								],
+							});
+							deferredResponse.resolve(
+								new Response(
+									`Could not proxy this request to your Worker: ${error.message}`,
+									{ status: 502 }
+								)
+							);
+							return;
+						}
+
 						void sendMessageToProxyController(this.env, {
 							type: "error",
 							error: {
@@ -284,6 +319,13 @@ function isRequestForLiveReloadWebsocket(req: Request): boolean {
 	return isWebSocketUpgrade && websocketProtocol === LIVE_RELOAD_PROTOCOL;
 }
 
+class UserWorkerFetchError extends Error {
+	constructor(cause: Error) {
+		super(cause.message, { cause });
+		this.name = "UserWorkerFetchError";
+	}
+}
+
 function sendMessageToProxyController(
 	env: Env,
 	message: ProxyWorkerOutgoingRequestBody
diff --git a/wrangler-dist/ProxyWorker.js b/wrangler-dist/ProxyWorker.js
index 2631ee69aa0d96a99d35e7e1d0535c50019c45ec..f05a82439d9cd8de5e23148a1cbad34a0c18987d 100644
--- a/wrangler-dist/ProxyWorker.js
+++ b/wrangler-dist/ProxyWorker.js
@@ -139,7 +139,21 @@ var ProxyWorker = class {
           headers.set(key, value);
         }
       }
-      void fetch(userWorkerUrl, new Request(request, { headers })).then(async (res) => {
+      void fetch(userWorkerUrl, new Request(request, { headers })).catch((error) => {
+        // One retry for a lost read connection; never replay writes or upgrades.
+        if (
+          (request.method === "GET" || request.method === "HEAD") &&
+          !request.signal.aborted &&
+          !request.headers.has("Upgrade") &&
+          error.message === "Network connection lost." &&
+          isSameUserWorkerOrigin(userWorkerUrl, this.proxyData?.userWorkerUrl)
+        ) {
+          return fetch(userWorkerUrl, new Request(request, { headers }));
+        }
+        throw error;
+      }).catch((error) => {
+        throw new UserWorkerFetchError(error);
+      }).then(async (res) => {
         res = new Response(res.body, res);
         rewriteUrlRelatedHeaders(res.headers, innerUrl, outerUrl);
         await checkForPreviewTokenError(res, this.env, proxyData);
@@ -154,6 +168,24 @@ var ProxyWorker = class {
         deferredResponse.resolve(res);
       }).catch((error) => {
         if (isSameUserWorkerOrigin(userWorkerUrl, this.proxyData?.userWorkerUrl)) {
+          if (error instanceof UserWorkerFetchError) {
+            void sendMessageToProxyController(this.env, {
+              type: "debug-log",
+              args: [
+                "Could not proxy request to the UserWorker:",
+                request.method,
+                request.url,
+                error.message
+              ]
+            });
+            deferredResponse.resolve(
+              new Response(
+                `Could not proxy this request to your Worker: ${error.message}`,
+                { status: 502 }
+              )
+            );
+            return;
+          }
           void sendMessageToProxyController(this.env, {
             type: "error",
             error: {
@@ -213,6 +245,12 @@ function isRequestForLiveReloadWebsocket(req) {
   const isWebSocketUpgrade = req.headers.get("Upgrade") === "websocket";
   return isWebSocketUpgrade && websocketProtocol === LIVE_RELOAD_PROTOCOL;
 }
+class UserWorkerFetchError extends Error {
+  constructor(cause) {
+    super(cause.message, { cause });
+    this.name = "UserWorkerFetchError";
+  }
+}
 function sendMessageToProxyController(env, message) {
   return env.PROXY_CONTROLLER.fetch("http://dummy", {
     method: "POST",
