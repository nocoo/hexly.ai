# Pi Agent Policy — first identity study

- Owner request: “新项目：nocoo/pi-agent-policy，执行一套补齐，包括logo，修正其readme，两边push，我们z+1”.
- Source: `nocoo/pi-agent-policy`, inspected at `352e8b9551e569f33f73273a20aa2ca66d06c2e5`; package `@nocoo/pi-agent-policy` 0.1.2. The repository has no existing independent Logo or UI palette. Do not invent a previous animal or website theme.
- Product: a user-level Pi extension for declarative tool policies and bounded semantic recovery. The default shares one repair budget per original user input. It is not an OS or network sandbox and does not guarantee exactly-once tool execution.
- Identity proposal: an armadillo, new to this catalogue. Its broad, layered shell suggests deliberate boundaries. Its natural small quadruped anatomy stays readable; the product description remains about the extension, not its mascot.
- Drawing: connected irregular flat polygons across the shell, head, ears, legs and tail. Broad shell planes, fewer smaller facial facets, crisp shared boundaries; no smooth plastic, glass shards, outlines or triangle confetti.
- Decisive moment: the armadillo pauses in a calm three-quarter side view, with its short head turned toward one small folded path ribbon near a front paw. The shell is the dominant compact mass. Keep the tail curved close to the body and the complete animal inside the square.
- Interest point: one short, folded coral/teal/saffron ribbon, with a single deliberate turn, extends into the small negative space in front of the paw. It stays subordinate and close to the body; no additional rainbow accessories, floating particles or typography.
- Palette proposal: warm mineral ochre, tawny amber and deep charcoal-brown anatomy; restrained multicolored ribbon. Sample actual returned pixels after selection. There are no source UI colors to replace.
- Composition: compact full-body character, intentional asymmetry, generous clear space for a rounded presentation. Broad anatomy, not a stretched tail or distant accessory, supplies visual fullness. Native square composition; no post-generation crop.
- Presentation proposal after raw approval: warm mineral-paper light field, charcoal-olive dark field, shallow contact shadow and a distinct interrupted armor-band motif. The background, texture and shadow will be separate layers and labelled as designed presentation colors.
- Ordered references: approved Frogie finishing 02 for connected flat facets; `ref01.jpeg` and `ref02.jpeg` for restrained presentation and balanced framing only. No source identity is supplied because none exists. Do not copy the frog, its green palette or its musical notes.
- Request: existing Azure OpenAI v1 helper, model `gpt-image-2`, high quality, one native 2048 × 2048 PNG; `sourceMode: "isolated-white"`. All anatomy and the ribbon remain opaque on uniform white. No tile, cast shadow, halo, external light or transparent-looking anatomy in the raw response.
- Provenance: archive the exact prompt, ordered reference hashes, sanitized request/response, native bytes and raw hash. Existing reference rights remain with their recorded sources. New authored supporting material uses the Hexly repository MIT license; typography, when introduced, keeps its font license.
- Authorization: source and Hexly updates/push are requested. The exact-byte raw-image checkpoint remains pending; no unseen image is considered owner approved. Repository visibility is a separately pending owner preference, not authorization to expose private code.

Show the untouched native result immediately. Do not extract, resize, compose,
publish, or integrate it before the owner confirms those exact bytes.
