# Pi Agent Policy — compact reset instrument, bounded retry

Owner instruction remains: “重新生成logo，chrome打开我审查”. The product,
material-series direction and compact push-to-reset design are documented in
`../2026-09-14-02/brief.md`.

The preceding call ended after 603.89 seconds with `TimeoutError: The read
operation timed out`. Its response record correctly says `outcome-unknown`:
there was no local image, and the server's completion or billing state is unknown.
The previous local process has exited; its prompt, request and response remain
unchanged. This is one deliberate bounded retry after inspecting that record,
not a concurrent call or an automatic retry of a still-running request.

Keep Azure OpenAI v1 `gpt-image-2`, high quality, and native 2048 × 2048 PNG.
Use only the approved Runner instrument as a material/framing reference to
reduce repeated reference upload. Do not copy its stopwatch or palette. The
prompt retains the same compact graphite/ivory instrument and terracotta reset
button, with a complete silhouette and pure-white extraction source. Neither
the rejected animal nor the unselected rocker is a model input.

The source is still `nocoo/pi-agent-policy` at
`352e8b9551e569f33f73273a20aa2ca66d06c2e5`, package 0.1.2: a user-level Pi
extension for declarative tool policies and bounded recovery. The tool is a
visual metaphor, not a hardware or security-isolation claim. No product website,
source adoption, original UI palette or prior official Logo is invented.

Preserve exact output bytes, model/request metadata, prompt and reference hash.
This is an AI-generated raster candidate; reference rights retain their existing
Hexly provenance. Open the untouched returned image in Google Chrome for the
owner's requested review. Raw acceptance remains pending until the owner selects
those exact bytes. Do not extract, resize, compose backgrounds or publish it yet.
