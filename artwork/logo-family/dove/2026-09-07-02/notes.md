# Dove framing refinement

**Withdrawn before publication.** The owner rejected forced neck elongation and requested natural anatomy with more compositional freedom. The original source tree is restored in `cb477a39d2fbb6bc561bcf5348cb5e830c83de29`. The local and published catalogue paths show the original identity; this candidate was never integrated. [Current owner feedback](../../2026-09-07-owner-feedback.json).

Archived study: `2026-09-07-02`, finishing `02`. [Complete static review](review.html) · [Local gallery](https://index.dev.hexly.ai/logos/dove) · [Published gallery](https://hexly.ai/logos/dove).

## Scope and acceptance

The owner retained this animal, expression, palette, and signature details, requested safer occupancy and natural square-edge continuation, and explicitly delegated intermediate raw acceptance for the five named projects. `approval-scope.json` preserves that instruction. `raw-review.json` records inspection of these exact native bytes under delegated authority; it does not claim the owner personally reviewed them.

The initial drawing used the original identity, approved Frogie, and both presentation boards. It was too full for safe rounded presentation. A uniformly scaled guide preserves that drawing's head and accessories; the current request uses only that archived guide to complete the lower anatomy. Each request, raw result, guide derivation, and diagnostic finishing pass remains in its own directory.

## Archived finishing

The native source is 2048 × 2048. All three master roles share the selected composition. The integer placement is `[0, 0]` px at 2048, with no finishing rescale. Lower-neck and shoulder intersections are intentional; a circular bust mask and repeated edge pixels are not used.

Pass 01 retained small exterior near-white fragments. Pass 02 calibrates the matte to the observed source, with minimum channel 237 and maximum chroma 16. It retains the accepted opaque colors, then adds a separate project-specific background and shallow shadows. The rounded radius is 23%.

Protected-feature clearance against the rounded boundary: head, beak and all blossoms: 221.5 px. Required minimum: 128 px. No protected feature is clipped. `checks/finishing-02.json` records the opaque RGB comparison, alpha preservation, integer placement, export dimensions, and removed component coordinates. Translated semitransparent edges may differ by one 8-bit RGB level through alpha compositing; opaque RGB and the alpha channel are unchanged.

Dove uses three explicit pale-anatomy protection regions and two seeded enclosed backdrop gaps. The native coordinate grids are archived in checks/. The white petals, light cere, four detached petals, and amber eye were inspected on both light and dark. Only one isolated antialias pixel was removed.

## Direction

### Blossoms beside the gaze

The amber eye and right-facing profile retain their direction. Blossoms frame the lower portrait loosely, while the neck continues past the bottom instead of ending in a round medallion.

### Cool plumage, warm blossoms

Slate, teal, and violet facets carry the iridescent plumage. Pink and ivory blossoms form the secondary focal point; pale petals and the light beak stay opaque and clean.

### A quiet flowering branch

An open petal-and-branch relief sits on a muted rose field. The motif follows the empty left side, leaving the bird and flowers clearly separated from the matte background.

## Palette and sizes

`palette.json` records actual native sRGB pixel coordinates, source SHA-256, and RGB/hex values. The designed `#c6a4b5` background and its `Petal branch relief` geometry remain separate from the existing `#c65379` website primary.

At 32 px, the side profile, amber eye, and pink blossom collar carry the mark. At 16 px, stamens and individual petals merge into a soft pink accent around the blue silhouette.

Transparent, square, and rounded PNGs are available at 2048, 1024, 512, 256, 128, 64, 48, 32, 24, and 16 px, plus a white master. The archived static review contains the previous identity, all three views, light/dark edge checks, real-size specimens, copyable colors, and exact-prompt/raw/master downloads. Presentation reference boards appear only in the static review and Git archive.

## Withdrawn source adoption

Local adoption `06f347fe8491990b5f27be86ea4f778a6dd35fca` was reverted by `cb477a39d2fbb6bc561bcf5348cb5e830c83de29` before any push or deployment. The source tree exactly matches baseline `cf62b6f2bc4c563eab271bf64fac4401ae4f918e`. The unreferenced public candidate backup was moved to `superseded-adoption/logo.png`; its provenance is alongside it. All raw and frozen finishing artifacts remain unchanged.
