# Showtime study 02

Azure gpt-image-2 returned one native 2048 × 2048 PNG in 127.01 seconds. The exact returned bytes are preserved in `raw/generated.png`, SHA-256 `0ff9ef4fa0f689a018f4dae092220dc6ebf80fe88828bdbc691081213bcf072a`. The request and response contain sanitized provenance; credentials were supplied through workflow's direnv environment.

The untouched result was immediately displayed with its native file link. It shows a matte charcoal clapperboard, warm-white diagonal inlays, a brushed-metal hinge and a modestly raised upper stick. SHOWTIME, ROLL, SCENE, TAKE and all three 01 values are legible and correctly spelled. The complete board and raised stick remain inside the native square. Its framing will need measured clearance against the final rounded outline. White inlays and lettering are solid object surfaces, not background holes. The visible backdrop is near-white; its actual matte must be sampled after selection.

The owner approved these exact raw bytes with “审查通过，继续”. `raw-review.json` records that decision and preserves the earlier pending state. Extraction and presentation work began after approval.

The selected finishing pass is **03**. The independent transparent foreground preserves all 2,149,115 fully opaque native pixels without any RGB changes; the broad slate and lettering have no alpha holes. The pale sage background uses film-gate registration brackets, perforation marks and editing cues, with the requested `#61BB46` green at 30% opacity. Its shadow and motif remain separate from the foreground. Uniform placement at scale 0.92 and offset [-8, 14] leaves 178.5 px minimum clearance against the actual rounded outline, with no clipped pixels.

Pass 01 failed clearance because of near-white exterior residue. Its partial outputs, failure record and inspection of all 31 tiny regions remain archived. Pass 02 completed but retained a connected pale fringe below the board, so it was superseded. Pass 03 broadens the exterior-white extraction range without removing object components. All passes and exact generation records remain intact.

The final archive includes transparent, square and rounded PNGs at 2048, 1024, 512, 256, 128, 64, 48, 32, 24 and 16 px, plus a native white presentation. The [inspection record](inspection/finishing-03.json) verifies dimensions, hashes, opaque artwork, clearance and the [actual-size sheet](inspection/sizes-03.png). At 32/24/16 px the striped clapstick and slate silhouette carry recognition; tiny lettering and material grain merge. The [static comparison](review.html) and [catalogue page](https://hexly.ai/logos/showtime) show the previous identity, all three presentations, palette, edge checks, sizes and exact prompt.

The owner assigned application icon replacement to Showtime's existing Codex in Herd, pane w2H:p1. The [handoff](handoff.json) supplies exact masters, roles, paths and hashes. That agent copied root `logo.png` and `logo-readme.png` byte-for-byte; this agent independently verified both hashes. Showtime Codex owns the Swift/macOS consumers, its separate icon commit and its own publication decision. This agent owns Chinese `README.md`, English `docs/README.en.md`, profile synchronization and Hexly v0.5.2 publication. The two READMEs use a 128 px header referencing the root rounded presentation.

README conventions were retrieved through workflow's `zhengli-update-github-readme` skill and nmem records `e8b8dd89-cdf9-4359-91bb-c5baf51fbca5` and `e42a5dde-192d-495f-bc21-4d303cabeb3a`. InfoSpace supplied an existing bilingual layout example. Features were checked against the published source and the source agent's report. The v1.0.0 release currently provides source only; neither README claims an application installer. Unfinished UI changes are excluded from the feature description.

README commit `9039ab8` changes only the two documents. Both have equivalent sections and a valid JSON film example. All documented CLI invocations parse, the eight MCP tool names match the implementation, and all 22 local links, including both root-image references, resolve. The matching GitHub profile addition was separately pushed at `73203ef18b98ccf7cf03be7ef0ebf327bb8a743d`. No app interaction or Swift edit was performed by this agent.

The owner then took over App follow-up and asked Hexly to publish directly. Application monitoring stopped; neither a source adoption commit nor an App release is a dependency of Hexly 0.5.2. The catalogue displays the approved finishing 03 and truthfully retains source-adoption status `review` until the source revision is confirmed. No native application adoption is claimed by this website publication.
