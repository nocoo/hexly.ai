# Hermes Gateway for Herdr — An attended gateway

This project supervises a dedicated Hermes Gateway inside a real Herdr pane, with persistent start/pause intent, identity checks, duplicate prevention and JSON diagnostics. The inspected local checkout contains an offline implementation; the public repository still contains the earlier design baseline. Real Herdr/Hermes integration has not been validated. Source provenance records both revisions and the existing unpublished commits without publishing them.

No independent original logo or application theme was found. The proposed identity is a compact tabletop gateway: a thick brushed-brass frame and a half-open petrol-blue enamel door, with one small green status lens mounted on the frame. A restrained ivory terminal prompt on the door connects the object to its actual CLI role. The door supplies the decisive gesture; the lens is the only small accent. This is a new material-series interpretation selected for this draft, not an existing or owner-approved identity.

Use an approximately 75 mm three-quarter product view. The broad door and substantial frame must form one full mark rather than a thin empty outline. Preserve the complete frame, hinge, door, status lens and low structural foot. The palette is designed for this study; it is not an inferred website theme. Physical enamel, brass and glass shading replace the animal-series facet rules.

## Ordered references

1. Signoff study `2026-09-07-01/raw/generated.png`: convincing compact object construction, restrained enamel and brass, broad lighting and clean edges. Do not transfer the counter, digits or button.
2. `references/ref02.jpeg`: compact framing, calm material hierarchy and the later presentation philosophy only. Do not transfer animals, colored backgrounds or pattern geometry into the source.

Request one native 2048 × 2048 opaque PNG through Azure gpt-image-2, high quality, with a uniform white matte. The open area through the gateway must contain the same white background. The green lens is solid colored glass with contained highlights; exclude external bloom, shadows, floor planes and atmospheric spill.

## Independent finishing plan

After exact-byte raw confirmation, extract the complete foreground and compose pale blue-gray paper with a distinct paired-pane and returning-route relief. Keep pattern, grain and projected shadows separate from the transparent master. Sample the actual native material pixels only after generation; no historical website tokens are claimed.

Preserve a clearly documented editorial gateway emoji as the before identity if no independent source image exists at adoption. Deliver all ten sizes, transparent/square/rounded/white masters, native palette evidence, complete static and catalogue reviews, source brand files, bilingual README files and the generated profile. Header/sidebar specimens are scale demonstrations: this project currently supplies a CLI and development manifest, not a web dashboard.

## Authorization

The current instruction is recorded verbatim in `authorization.json`. No intermediate raw-review waiver has been given for this batch. Show the untouched image immediately and keep the exact bytes pending until the owner confirms them. README research can proceed independently. The task does not authorize starting a real Gateway, installing a plugin, modifying a user's Hermes Profile or publishing the existing eleven local implementation commits. Source branding and site preparation stay local unless publication is subsequently requested.
