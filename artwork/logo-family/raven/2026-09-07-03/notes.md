# Raven framing refinement

**Withdrawn before publication.** The owner preferred study 02 and rejected the left-side crop of study 03. This concerns the study and its finished presentation, not a claimed new inspection of the untouched raw. The original source tree is restored in `d47240aad21131f74d6d7607fada76ad53a35baa`. The local and published catalogue paths show the original identity; this candidate was never integrated. [Current owner feedback](../../2026-09-07-owner-feedback.json).

Archived study: `2026-09-07-03`, finishing `02`. [Complete static review](review.html) · [Local gallery](https://index.dev.hexly.ai/logos/raven) · [Published gallery](https://hexly.ai/logos/raven).

## Scope and acceptance

The owner retained this animal, expression, palette, and signature details, requested safer occupancy and natural square-edge continuation, and explicitly delegated intermediate raw acceptance for the five named projects. `approval-scope.json` preserves that instruction. `raw-review.json` records inspection of these exact native bytes under delegated authority; it does not claim the owner personally reviewed them.

The initial drawing used the original identity, approved Frogie, and both presentation boards. It was too full for safe rounded presentation. A uniformly scaled guide preserves that drawing's head and accessories; the current request uses only that archived guide to complete the lower anatomy. Each request, raw result, guide derivation, and diagnostic finishing pass remains in its own directory.

## Archived finishing

The native source is 2048 × 2048. All three master roles share the selected composition. The integer placement is `[-106, 0]` px at 2048, with no finishing rescale. Lower-neck and shoulder intersections are intentional; a circular bust mask and repeated edge pixels are not used.

Pass 01 retained small exterior near-white fragments. Pass 02 calibrates the matte to the observed source, with minimum channel 237 and maximum chroma 16. It retains the accepted opaque colors, then adds a separate project-specific background and shallow shadows. The rounded radius is 23%.

Protected-feature clearance against the rounded boundary: crown, eye and projecting beak: 173.5 px. Required minimum: 128 px. No protected feature is clipped. `checks/finishing-02.json` records the opaque RGB comparison, alpha preservation, integer placement, export dimensions, and removed component coordinates. Translated semitransparent edges may differ by one 8-bit RGB level through alpha compositing; opaque RGB and the alpha channel are unchanged.

No isolated foreground component was removed in the selected finishing pass.

## Direction

### A profile that enters the frame

The long projecting beak balances the rear feather mass. The neck enters naturally from the left and lower edges, while the crown, eye, and beak keep a clear rounded-square margin.

### Indigo with cool flashes

Broad indigo planes retain the original profile, with cyan, mint, and violet flashes across the feathers. The faceted eye remains the focal point; the existing projecting beak supplies the outward gesture.

### Feather vanes at dusk

Offset angular feather vanes cross a cool dusk-blue field. Their quiet relief and soft shadow give the dark portrait separation without adding another object to the mark.

## Palette and sizes

`palette.json` records actual native sRGB pixel coordinates, source SHA-256, and RGB/hex values. The designed `#969cbf` background and its `Offset feather vanes` geometry remain separate from the existing `#6341c8` website primary.

At 32 px, the long beak and indigo-cyan feather planes remain readable. At 16 px, the eye facets and smaller feather divisions merge into the profile.

Transparent, square, and rounded PNGs are available at 2048, 1024, 512, 256, 128, 64, 48, 32, 24, and 16 px, plus a white master. The archived static review contains the previous identity, all three views, light/dark edge checks, real-size specimens, copyable colors, and exact-prompt/raw/master downloads. Presentation reference boards appear only in the static review and Git archive.

## Withdrawn source adoption

Local adoption `dc7b155fd3e6da627fed123014ba8222b6950b18` was reverted by `d47240aad21131f74d6d7607fada76ad53a35baa` before any push or deployment. The source tree exactly matches baseline `3582a6309e18e7c30d6abcf25c1d9884d3852281`. The unreferenced public candidate backup was moved to `superseded-adoption/logo.png`; its provenance is alongside it. All raw and frozen finishing artifacts remain unchanged.
