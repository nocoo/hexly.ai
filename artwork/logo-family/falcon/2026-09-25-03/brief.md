# Falcon head study

The owner rejected the previous output: too much body, insufficient off-center
composition and weak distinction between birds. The new request names only the
species (peregrine falcon), head focus, asymmetry, multicolored accents and
connected fragmented drawing. Camera, color, gesture and accent are left to the
image model. No rejected output is supplied as an identity reference.

Off-center design lets the head enter a viewfinder with deliberate negative
space carrying the counterweight. The previous broad shoulder/chest prescription
is withdrawn. Existing board 02 is reference 1; board 01 is reference 2; approved
Frogie finishing 02 is reference 3 for drawing only. Their exact hashes are saved
by the request helper.

Model: `gpt-image-2.5-sunburst`. Source mode: `isolated-white`. Native request:
2048 × 2048. Exact-byte owner acceptance is required before any finishing.
The full owner correction is preserved in the preceding study's raw review.
