# Four-project framing audit

Wooly, Noheir, Raven, and Life.ai retain their liked animals and accents while replacing hard bust cuts with natural viewfinder entries. The owner delegated intermediate acceptance for this batch. There were four Azure gpt-image-2 requests, one per project, each at native 2048 × 2048.

## Evidence

- `source-revisions.json` and `source-publication.json`: exact source adoption commits, completed pushes, and successful CI; Wooly and Noheir also completed their existing Release workflows.
- `source-assets.json`: three exact masters per source project, every transparent PNG, both ICO sizes, square touch/install icons, rounded social presentations, and README targets.
- `source-sidebar-review.json`: actual source sidebar components rendered with explicit router/session fixtures and compiled source CSS. Both sidebar states and both themes were inspected without starting application backends. Raven renders its 24 px source at 20 CSS px; the other three render it at 24 CSS px.
- `source-login-review.json`: Life.ai's production build on an isolated loopback server. The login mark is transparent at 96 CSS px, the header mark at 16 CSS px, and effective browser/touch icon links return their selected assets.
- `browser-review.json`: four individual site paths and four static study pages across English/Chinese, light/dark, and desktop/mobile. Before/after, all three modes, exact prompt, downloads, copyable palette, actual-size marks, navigation, and overflow checks pass. The Universe uses six Refined presentations.
- `screenshots/` and `source-browser/`: browser captures; the source sidebar captures contain fixture identity only.

Each study preserves the untouched generation, exact prompt, request/response, ordered references, every finishing pass, and palette samples. Its `verification/framing.json` names the conservative protected regions, actual rounded-boundary clearance, and separate shoulder/wing exceptions. All protected features exceed 128 native pixels of clearance with zero clipping. `verification/matte.json` confirms that every fully opaque extracted pixel retains the raw RGB values. Native continuation layers preserve opaque placed source pixels and are archived without formatting.

## Site validation

- Source checksums and previews: 66 projects, 264 display derivatives.
- Archives: 19 public family manifests and every recorded file in 46 finishing passes.
- Typecheck and full lint: passed.
- Unit coverage: 100% statements, branches, functions, and lines.
- Repository Chromium suite: 64 browser cases passed.
- HTTP, security, Workers dry run, and publication are completed by the scoped site push and exact-commit CI deployment.

The publication checkout carries only this authorized batch and the Refined Universe change. Earlier unpublished source/logo work stays in the local history.
