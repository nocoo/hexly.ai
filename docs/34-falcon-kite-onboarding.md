# Falcon and Kite onboarding

Status: The owner approved both revised native head portraits and authorized
finishing, skill correction and publication. Falcon selects finishing `02` of
study `2026-09-25-03`; Kite selects finishing `03` of study `2026-09-25-02`.
Brand-only source commits are prepared in isolated worktrees. Catalogue,
material publication, complete dev review and the Hexly release are being
completed separately; this record does not yet claim a production deployment.

## Source intake

| Project | Initial observed state | Preserved evidence |
| --- | --- | --- |
| Falcon | Design-only native macOS Jev proxy and seven-day decision observability app; no runnable application | [Intake](sources/falcon-2026-09-25.json), including local README, project instructions, product and interface documents |
| Kite | Pi extension research and an untracked collector contract; final visualization client is not evidenced | [Intake](sources/kite-2026-09-25.json), including collector and version-specific Pi research |

The initial source snapshots above are immutable intake records, not a statement
that source development has stopped. Falcon subsequently gained a Swift
application bootstrap; Kite gained a local collector implementation. Publication
intake records distinguish those observed capabilities from planned UI.

Both GitHub repositories are public. Their descriptions and homepages were empty
at intake. Inspected source content includes unpublished local work, so public
source revisions stay null. Exact files are preserved under `docs/sources/` with
hashes; local HEADs are evidence, not assertions of publication. Existing source
commits and untracked files remain owned by their development workflows.

No independent Logo was found. These are first identities, with `previous: null`
after acceptance. Do not invent a previous emoji drawing or a replacement story.

## Reviewed content direction

| Field | Falcon | Kite |
| --- | --- | --- |
| Slug / route | `falcon` / `/projects/falcon` | `kite` / `/projects/kite` |
| Category | Tools | Tools |
| Profile emoji | 🦅 | 🪶 |
| English description | Native macOS Jev proxy and decision observability, in development | Local Pi execution-trace collection for inspection and visualization |
| Chinese description | 开发中的 macOS 原生 Jev 代理与决策观察应用 | 面向检查与可视化的本地 Pi 执行轨迹采集 |
| Destination | `https://github.com/nocoo/falcon` | `https://github.com/nocoo/kite` |
| Website / health | No verified independent website | No verified independent website |

Falcon's overview must distinguish its planned HTTP/MCP source-key management,
168-hour history, decision review, usage charts and local read-only replay from
implemented capabilities. Proposed SwiftUI/Swift Charts, URLSession, Hummingbird,
MCP and GRDB choices are design recommendations, not installed dependencies or
measured performance. The proposed runtime SDK choice remains a source-product
decision and is not resolved by branding.

Kite's overview describes a passive observer: Pi remains the interactive CLI;
the collector neither sends prompts nor chooses tools or answers confirmations.
The current collector uses a local Unix socket and SQLite; preserve the
publication snapshot as evidence for implemented behavior. Its bounded,
fail-open observer contract must stay distinct from a completed visualization UI. Traces reflect observed events and explicit loss boundaries,
not complete hidden reasoning or complete visibility into arbitrary child agents.
The research references Pi 0.87.1; a native macOS application is not inferred from
the owner's bird-series choice.

No product screenshots, app downloads, performance scores, production services
or implemented UI claims are supplied at this stage. Native/local projects
without a website need no synthetic web header, health endpoint or monitor.

## Identity direction and checkpoints

The owner approved the proposed species, scope and first-round design plan with
“开始进行。完成后dev review”. This authorizes generation and local preparation;
it does not waive exact-byte raw acceptance.

- Falcon: Distinctive peregrine falcon head, strongly off-center, multicolored
  accents and connected flat fragments.
- Kite: Distinctive red kite head, strongly off-center, multicolored accents and
  connected flat fragments.
- The owner's correction withdraws the prescribed chest/wing structure, camera,
  palette, fan and ribbon. Let the image model choose these details. Presentation
  boards now lead the references; Frogie supplies drawing language only.
- Primary identity: `gpt-image-2.5-sunburst`; three ordered family references,
  native 2048-square request, uniform white isolated source.
- Decorative surfaces after identity selection: `gpt-image-2.5-flare`, independent
  light/dark habitat studies with exact-byte approval and separate pack versions.

Raw results are immediately shown to the owner. Preserve every request, unknown
outcome and rejected image. Only an approval matching the exact raw hash permits
extraction, independent backgrounds, ten export sizes or catalogue integration.

Falcon study `2026-09-25-01` ended with an unexpected TLS EOF and no image after
10.02 seconds; its remote outcome is unknown. An unauthenticated service-origin
HEAD subsequently returned HTTP 200. Study `2026-09-25-02` is a deliberate new
request with the same prompt/model/references, not an overwritten receipt or an
automatic retry. No claim is made about billing for the first request.

| Project | Successful study | Native source SHA-256 | Current decision |
| --- | --- | --- | --- |
| Falcon | `2026-09-25-02` | `90e973c7e3c53e34d8156ceefd4ad32e3e69a411dfb4737126089384dc0254a8` | Rejected by owner |
| Kite | `2026-09-25-01` | `8daa5cb7e6044242a7396490550baa40f86aa2539686a4e89f220afe1404657c` | Rejected by owner |

Both rejected outputs are native 2048 × 2048 PNGs. Neither was extracted,
derived or integrated. Falcon has a larger friendly eye and a left-shoulder frame
entry. Kite's ribbon is longer than intended and a tail feather approaches the
lower-right corner; the forked-tail cue is not clearly verified. The owner found
both bodies too large, their asymmetry insufficient and their bird identities
too similar. Exact feedback is preserved beside both rejected hashes.

The replacement studies are Falcon `2026-09-25-03` and Kite `2026-09-25-02`. Their prompts
are 88 and 90 words respectively, including reference roles and output format.
Neither rejected image is supplied as an identity reference for the new request.

| Project | Head study | Native source SHA-256 | Current decision |
| --- | --- | --- | --- |
| Falcon | `2026-09-25-03` | `2d4fd9e0f5abffc0dcebcd2e4880f685897ffc5137304b6d579f282faf110ca7` | Approved by owner; exact bytes reviewed |
| Kite | `2026-09-25-02` | `417279a8e8a0901b22dc42fe38c7eee3a6b090c67617fceaf7b2d312b45047e9` | Approved by owner; exact bytes reviewed |

Both are native 2048-square images with pronounced opposing edge-entry
composition. Both use open beaks and restrained multicolor, with no separately
generated external accent. The simplified owner direction supersedes earlier
accessory prescriptions. The exact owner approval is preserved in each
`raw-review.json`: “这两张很好，可以继续，修正你的skill，减少干预。以后对动物和鸟类型都照此办理。修正skill后继续上线操作”.

The shared `zhengli-project-logo` skill and prompt template were corrected in
Workflow commit `c0c2e362af3b14a89a42e876cc0f6bbc7a37b55b`: short species/head/off-center/multicolor/fragment briefs,
with no mandatory accessory, camera, eye count, body ratio or fixed palette.
Active Hexly direction documents follow the same rule; historic studies remain
unchanged.

Falcon finishing `02` preserves native scale and left/bottom entries on a slate
cliff-grass field. Kite finishing `03` uses uniform 90% placement at `[103,103]`
to protect its upper feather tip under the rounded presentation, retaining
right/bottom entries and its unscaled extraction archive. Its warm field uses
woodland-edge leaves. Both separate transparent identity pixels from authored
backgrounds and export ten sizes from 2048 to 16 pixels. The static reviews use
one first-identity specimen, exact prompt/reference disclosures, copyable native
palette evidence and illustrative app/browser roles; these are not product UI
screenshots.

Brand-only source commits `aa0e050` (Falcon) and `704ae38` (Kite) contain the
transparent root Logo, square/rounded masters and usage documentation. They were
created from remote `main` in isolated worktrees. Publishing these commits does
not publish the active source checkouts' unrelated application commits or
uncommitted development. Source adoption and public reachability are verified
separately from the Hexly release.

## Local delivery and publication boundary

1. Completed: approve raw Logos and preserve exact decisions beside each request.
2. Finish separate foreground/presentation masters, ten export sizes, native
   palettes and role-correct app/favicon specimens.
3. Complete individual static review pages and canonical project detail pages,
   including first-identity history, both themes, all downloads and exact prompts.
4. Review independent light/dark botanical texture originals before export and
   integration. Logo approval does not approve unseen texture bytes.
5. Add catalogue/index/order records and generated profiles, retaining existing
   numbered profiles and source provenance. Keep source adoption distinct from a
   local candidate and from the future public catalogue release.
6. Prepare matching GitHub profile entries and repository descriptions. Local
   profile commits are not remote publication; preserve unrelated source work.
7. Verify source hashes, asset checks, type/lint/build, repository quality gates,
   desktop/mobile themes, navigation, downloads and original-byte delivery.
8. Deliver local review at `https://index.dev.hexly.ai/projects/falcon` and
   `https://index.dev.hexly.ai/projects/kite`, with actual execution evidence.

Static study verification passed on 2026-09-25 in Chromium through a temporary
loopback HTTP server: Icon/Transparent/White switching, theme switching, palette
clipboard copying, exact prompt text, image loading, every study-relative link
and download, and 390/1280 px layouts without horizontal overflow. Both pages
returned zero HTTP errors. Biome and `git diff --check` passed for this scoped
review/documentation work. Canonical application review and production evidence
remain separate gates.

R2 publication and the normal exact-revision Hexly release follow the reviewed
result within the session's publication authorization. Binary materials remain
outside Git and deployed Static Assets. Source-application implementation and
distribution are separate work; do not push their existing unpublished commits
as a side effect of brand preparation.

## Optional decorative texture checkpoint

Four native Flare canvases were shown to the owner after generation and remain
`pending` in their individual raw-review records. They are not used in the
catalogue or exported into production texture packs without an exact-byte
decision. The first brand kits use the separately authored habitat linework
from the approved Logo finishing recipe. This does not hold the authorized
Logo/catalogue publication.
