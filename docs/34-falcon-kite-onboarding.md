# Falcon and Kite onboarding

Status: Both native first-identity images have returned and were shown to the
owner. Source snapshots and generation receipts are recorded. Exact-byte raw
acceptance, finishing, catalogue integration and complete dev review remain
pending. No production release or source-application release is claimed.

## Source intake

| Project | Observed state | Preserved evidence |
| --- | --- | --- |
| Falcon | Design-only native macOS Jev proxy and seven-day decision observability app; no runnable application | [Intake](sources/falcon-2026-09-25.json), including local README, project instructions, product and interface documents |
| Kite | Pi extension research and an untracked collector contract; final visualization client is not evidenced | [Intake](sources/kite-2026-09-25.json), including collector and version-specific Pi research |

Both GitHub repositories are public. Their descriptions and homepages were empty
at intake. Inspected source content includes unpublished local work, so public
source revisions stay null. Exact files are preserved under `docs/sources/` with
hashes; local HEADs are evidence, not assertions of publication. Existing source
commits and untracked files remain owned by their development workflows.

No independent Logo was found. These are first identities, with `previous: null`
after acceptance. Do not invent a previous emoji drawing or a replacement story.

## Reviewed content direction

| Field | Falcon | Kite |
| --- | --- | --- |
| Slug / route | `falcon` / `/projects/falcon` | `kite` / `/projects/kite` |
| Category | Tools | Tools |
| Profile emoji | 🦅 | 🪶 |
| English description | Planned native macOS Jev proxy and decision observability | Local Pi execution tracing and visualization |
| Chinese description | 规划中的 macOS 原生 Jev 代理与决策观察应用 | 面向 Pi 的本地执行轨迹采集与可视化项目 |
| Destination | `https://github.com/nocoo/falcon` | `https://github.com/nocoo/kite` |
| Website / health | No verified independent website | No verified independent website |

Falcon's overview must distinguish its planned HTTP/MCP source-key management,
168-hour history, decision review, usage charts and local read-only replay from
implemented capabilities. Proposed SwiftUI/Swift Charts, URLSession, Hummingbird,
MCP and GRDB choices are design recommendations, not installed dependencies or
measured performance. The proposed runtime SDK choice remains a source-product
decision and is not resolved by branding.

Kite's overview describes a passive observer: Pi remains the interactive CLI;
the collector neither sends prompts nor chooses tools or answers confirmations.
Its proposed bounded exporter uses a local Unix socket and transaction-backed
acknowledgements. Traces reflect observed events and explicit loss boundaries,
not complete hidden reasoning or complete visibility into arbitrary child agents.
The research references Pi 0.87.1; a native macOS application is not inferred from
the owner's bird-series choice.

No product screenshots, app downloads, performance scores, production services
or implemented UI claims are supplied at this stage. Native/local projects
without a website need no synthetic web header, health endpoint or monitor.

## Identity direction and checkpoints

The owner approved the proposed species, scope and first-round design plan with
“开始进行。完成后dev review”. This authorizes generation and local preparation;
it does not waive exact-byte raw acceptance.

- Falcon: Slate-blue peregrine portrait, warm ivory chest and one small
  multicolored folded fan in its gaze. Separate rocky-ledge grass presentation.
- Kite: Rust/chestnut red kite, natural backward glance, folded wings and one
  short multicolored ribbon. Separate woodland-edge foliage presentation.
- Primary identity: `gpt-image-2.5-sunburst`; three ordered family references,
  native 2048-square request, uniform white isolated source.
- Decorative surfaces after identity selection: `gpt-image-2.5-flare`, independent
  light/dark habitat studies with exact-byte approval and separate pack versions.

Raw results are immediately shown to the owner. Preserve every request, unknown
outcome and rejected image. Only an approval matching the exact raw hash permits
extraction, independent backgrounds, ten export sizes or catalogue integration.

Falcon study `2026-09-25-01` ended with an unexpected TLS EOF and no image after
10.02 seconds; its remote outcome is unknown. An unauthenticated service-origin
HEAD subsequently returned HTTP 200. Study `2026-09-25-02` is a deliberate new
request with the same prompt/model/references, not an overwritten receipt or an
automatic retry. No claim is made about billing for the first request.

| Project | Successful study | Native source SHA-256 | Current decision |
| --- | --- | --- | --- |
| Falcon | `2026-09-25-02` | `90e973c7e3c53e34d8156ceefd4ad32e3e69a411dfb4737126089384dc0254a8` | Pending owner confirmation |
| Kite | `2026-09-25-01` | `8daa5cb7e6044242a7396490550baa40f86aa2539686a4e89f220afe1404657c` | Pending owner confirmation |

Both returned native 2048 × 2048 PNGs. No extraction, derivative or integration
has been performed. Falcon has a larger friendly eye and a left-shoulder frame
entry. Kite's ribbon is longer than intended and a tail feather approaches the
lower-right corner; the forked-tail cue is not clearly verified. These inspection
notes are recorded with pending decisions, not substituted for owner approval.

## Local delivery and publication boundary

1. Approve raw Logos; preserve exact decisions beside each request.
2. Finish separate foreground/presentation masters, ten export sizes, native
   palettes and role-correct app/favicon specimens.
3. Complete individual static review pages and canonical project detail pages,
   including first-identity history, both themes, all downloads and exact prompts.
4. Add independent light/dark botanical texture packs after their raw checkpoint.
5. Add catalogue/index/order records and generated profiles, retaining existing
   numbered profiles and source provenance. Keep source adoption distinct from a
   local candidate and from the future public catalogue release.
6. Prepare matching GitHub profile entries and repository descriptions. Local
   profile commits are not remote publication; preserve unrelated source work.
7. Verify source hashes, asset checks, type/lint/build, repository quality gates,
   desktop/mobile themes, navigation, downloads and original-byte delivery.
8. Deliver local review at `https://index.dev.hexly.ai/projects/falcon` and
   `https://index.dev.hexly.ai/projects/kite`, with actual execution evidence.

R2 publication and the normal exact-revision Hexly release follow the reviewed
result within the session's publication authorization. Binary materials remain
outside Git and deployed Static Assets. Source-application implementation and
distribution are separate work; do not push their existing unpublished commits
as a side effect of brand preparation.
