# Diorama Journey onboarding

Diorama Journey is the first project in the dedicated `skills` category. Its
identity belongs to the `material` 3D tool series. Existing Skills & MCP entries
stay in `extensions`; this addition does not migrate or redesign them.

The catalogue now contains 76 projects: 56 active, 20 archived, and 62 with
independent original images. The entry is appended, preserving previous order
and numbered profiles. The existing GitHub profile entry already matches the
repository description; its immutable revision is
`f7fb0554c24d243f4b1aa9087403fa56125c17d0`.

## Product evidence and identity

The skill creates narrated miniature 3D stories from source material. A recurring
element joins chapters on one timeline, producing a film, chapter MP4s, subtitles
and PPTX/PDF/ODP slides with speaker notes. The original example follows one
amber lantern through a gateway, lookout, workshop, bridge and garden.

Study `2026-09-17-01 / 01` uses that example as its primary reference. The approved
Sunburst image contains a cream gateway, terracotta cap, sage island and model
trees, stone path, golden ring and amber lantern. Continuous physical materials
follow the tool direction. The former README emoji is not a previous independent
Logo, so `family.previous` is null.

| Record | SHA-256 / revision |
| --- | --- |
| Native 2048² Sunburst output | `068e443f48b66c4508508b2ea597251eb688f082807e06ae579f4925601088c2` |
| Adopted transparent 2048² master | `fff0d40b6bf3067f80efbbcf64e78c04a2e614e9bf83f9d8edbe2168246ec851` |
| Source adoption commit | `5a89d9da4d696d812a0aa46c58b94d81b8581b18` |

The foreground preserves all 1,927,450 fully opaque native pixels. The arch and
gold-ring openings are transparent; the physical paver behind the lantern handle
stays opaque. Uniform 0.94 placement leaves 212.5px minimum rounded-edge clearance
and clips no visible pixels. Ten export sizes span 2048px to 16px.

The source viewer retains its own evidenced colors: `#af7850`, `#f7f5ee` and
`#35463e`. The Logo palette is sampled from native artwork. Sage presentation
paper and Hexly campaign colors are recorded separately.

## Source adoption

Both language READMEs use the rounded Logo centered at 128px above the centered
project title. Their body text, badges, sections and illustrations are unchanged.
The actual viewer header displays a 48px transparent source at 24 CSS px. The
favicon contains verified transparent 16/24/32/48/64/128/256 PNG entries.

Source CI `35162989446` and the existing example deployment `35163240333` both
succeeded at the adoption SHA. Public header Logo and favicon responses match
the selected source bytes. No movie was regenerated for this Logo update.
The starter package remains `0.1.0`; its version is independent of Hexly's release.

The GitHub Pages example initially returned 404 and became available during
onboarding. Both observations are retained. It demonstrates output from an
installable skill; the catalogue uses the repository as the installation
destination. This Logo-only update does not introduce an origin-level health
migration or a synthetic monitor for generated example content.

## Materials and review

- Canonical page: `/projects/diorama-journey`, with the complete archive at `#brand`.
- Static identity study: `artwork/logo-family/diorama-journey/2026-09-17-01/review.html`.
- Brand package: `/brands/diorama-journey/v1.0.0/`, assembled from the approved
  foreground with zero additional primary-image generation calls.
- Typography: real Space Grotesk 600 outlines with full descenders and OFL notices.
- Wide and mobile Heroes: separate complete compositions, without cropping or
  recoloring the project Logo.
- Original example gallery: `/screenshots/diorama-journey/example/v1.0.0/`, retaining
  the source JPEG and whole-canvas 1600/320px WebP delivery files.
- Supporting Flare study: `artwork/brands/diorama-journey/texture-studies/2026-09-17-flare-01/`.
  The two native outputs remain pending raw acceptance, with no production
  derivatives or active texture-pack reference. This release uses the approved
  Logo presentation's authored working-sheet motifs. Study originals remain
  archived separately from adopted materials.

R2 publication uses only this project's materials in `hexlyai` at `h.no.mt`.
Inventory, full-byte verification and publication receipts precede the site
release. Material binaries stay outside Git and the Worker deployment.

The screenshot's original, preview and thumbnail are registered in the loose
asset route map. Dev responses return the image MIME instead of the application
HTML fallback. Desktop/mobile checks in both themes decoded the full 1600×900
preview and original, opened and closed the Lightbox, and found no page overflow
or browser errors.

## Example film

The detail's media section includes **A small idea, shared** at
`#video-a-small-idea`, followed by the existing screenshot gallery. Its original
1920×1080 H.264/AAC movie is byte-identical to the live GitHub Pages example and
the verified `v0.1.0` release archive. The picture lasts 68.1 seconds; the audio
and MP4 container last 68.2 seconds. The file keeps its original full-range
4:2:0 encoding and faststart layout.

The 37 KB WebP poster preserves the whole original HTML-rendered cover from
frame 150. The original PNG and source MIT notice are archived with the film.
All 11 original English subtitle cues retain their text and timing in WebVTT;
native captions start off because the film already contains visible captions.
The shared player loads the movie after a click and provides native controls
and inline playback.

Movie, cover, poster, captions and license use the independent R2 media version
`1.0.0`. Full CDN checksums, MIME, three permitted origins and a real `206`
range response are recorded in the
[film receipt](media/diorama-journey/a-small-idea/v1.0.0.json).

Dev review passed desktop and emulated mobile in both themes, including unmuted
audio/video decoding, seeking, real timed captions, English/Chinese labels and
the screenshot Lightbox. No horizontal overflow or page errors were observed.
The first playback-progress check timed out once; isolated diagnosis and the
subsequent complete four-configuration review passed unchanged. Validation also
passed 108 model tests, 70 HTTP checks, 20 media browser tests, lint, asset checks
and the production build. Profile regeneration left all 76 profiles unchanged.

## Publication state

The owner paused this site's publication on 2026-09-17 to add the example film.
Named R2 materials are available for local review; the website remains unpushed
and undeployed by this work. Resume the release only after a new owner instruction.

The remote baseline advanced to `0.15.0` during this work, so the requested Z+1
release is now `0.15.1`. The original `0.14.1` target remains historical intake
evidence. The existing emoji objects and their public routes stay available;
this adoption uses new preview and social-image paths. Open
`https://index.dev.hexly.ai/projects/diorama-journey` for the required development
review before publication. Source approval and source publication do not claim
that this site has already been released.

See the [source snapshot](sources/diorama-journey-brand-2026-09-17.json),
[source publication evidence](../artwork/logo-family/diorama-journey/2026-09-17-01/source-publication.json),
[screenshot receipt](assets/diorama-journey/screenshots/example/v1.0.0.json),
and [texture intake](brand-textures/2026-09-17-diorama-journey/inventory.json).
