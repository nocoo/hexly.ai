# DreamRO 主代理复核

2026-09-08，基线 `07185e854a59e320f8b2a45f6d50484186753c57`。批准双语 README 与 overview；正常 main 提交，再次 pull 后 push，保持版本 1.0.0。

## 复核依据

- 全文核对中英文 README，读取 manifest、Vite / Playwright / Workers 配置、素材声明、地图 / 职业 / 任务数据、存档与偏好实现，以及战斗技能、药水、强化、复活、伙伴与窗口暂停的主要代码。
- 这是浏览器单人 RPG，Three.js / WebGL 2 绘制三维场景，Web Audio 合成声音，Worker 仅托管静态资源。没有服务端账号、数据库或多人同步。
- 职业可直接选择且每类配置四技能，技能的治疗、护盾、伤害、范围、毒、陷阱、冲刺与召唤分支实际执行。三地图名称与源码一致，顺序任务完成后伙伴状态随存档保留。
- 等级、背包与任务在复活后保留；各角色与偏好存于当前 origin 的 localStorage。自动保存、设置内手动保存与打开窗口暂停均有实现。文稿准确说明跨域名 / 浏览器 / 设备不共享存档。
- npm ci 对应现有 lockfile，构建包含 TypeScript 与版本 / SHA 元数据。开发端口非 strict，可自动递增；不同端口的存档分开。没有虚构 API key 或初始化数据库步骤。
- 仅有 Playwright 测试入口，配置为 5188、macOS Metal / Linux SwiftShader；非 CI 可复用服务，文稿要求保持端口空闲。没有编造单元或 API 层，也未把用例列表当作执行通过。

## 验证

代理已完成生产构建与测试发现，保留已有 Three.js chunk 提示；主代理静态检查通过，图片 / 链接 / 代码块和双语示例一致。本次为文档变更，使用仓库现有 CI 执行完整浏览器流程，发布后补精确 SHA、远端文稿与 CI / 自动 Release 结果。
