# Pika 主代理复核

2026-09-08，基线 `02487858f8c567a7e3c780cf951abc957c54623d`。批准当前四份文档与 overview，保持正常 hooks，提交后再次 pull 并 push main；不改版本、不发布 npm。

## 复核依据

- 完整核对中英文 README、文档索引、开发说明、workspace manifests、CLI 注册与配置、同步管线、Web 路由、Worker 认证与存储实现，以及测试和发布入口。
- 当前是单个 Hono Worker 服务 React SPA 和 API，D1 保存元数据及 FTS5，R2 保存 gzip 标准化消息与原始内容。没有沿用旧文档的 Next.js 或双 Worker 拓扑。
- CLI 浏览器 loopback 登录、生产 / 开发固定地址和共用游标文件都有源码依据。已发布 npm 版本与源码版本不同，安装示例所用参数另有已发布 tarball 核验，未把源码现状冒充新发布版本。
- `--no-upload` 仍保存解析后游标，之后普通同步可能跳过未变化文件；上传失败只回退相关文件的本地游标。文档没有承诺无副作用预览、整体事务回滚或自动脱敏。
- `/api/ingest/*` Bearer 例外与其他业务路由的 Access 要求明确区分；CLI 管理命令的生产认证缺口、macOS 默认发现路径、回收站占位页面均如实说明。
- 默认开发 D1 / R2 的 remote=true 与本地 API runner 的显式 --local、独立持久化目录、测试身份和清理行为相符；根 TS、Web TSX、Bun 内存迁移测试的入口分别准确。
- Release 等待成功的 main CI、检出对应 SHA、构建并部署，不执行数据库迁移；200 / 401 健康检查只证明可达。站点和 MIT 链接有效。

## 修订与验证

主代理将开发说明的 Vite 默认代理地址精确改为源码中的 `127.0.0.1:8787`，并澄清调查记录中 CI 与 Release 的依赖关系。双语命令、相对链接、基线证据、空白检查通过。回读本地 API E2E 与 Web 测试日志均通过；完整 coverage 与提交 / 推送检查由正常 hooks 执行。未扫描个人会话或调用真实登录、同步和远程迁移。
