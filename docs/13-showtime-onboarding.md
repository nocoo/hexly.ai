# Showtime onboarding and classic clapperboard identity

Showtime turns scripted interactions with real webpages into macOS product demo movies. SwiftUI and AppKit provide the studio, WebKit hosts the pages, AVFoundation exports MP4, and Python exposes CLI and MCP control. Current exports have no audio track.

## Local storefront

The owner requested a preparatory entry before the project's first push. At that intake, the public `nocoo/showtime` repository was verified as empty. The inspected source files are copied into the [study inputs](../artwork/logo-family/showtime/2026-09-10-01/input/catalogue/), indexed by the [checksummed source snapshot](sources/showtime-2026-09-10.json). That historical snapshot records the actual theme tokens and each file's SHA-256. Its null revision states that there was no inspected commit at capture; the historical evidence remains intact.

The source project subsequently published `1fa4de8686166d4477ab78c7f3356aa8e19b403d` and v1.0.0. The [second study's source capture](../artwork/logo-family/showtime/2026-09-10-02/sources.json) records files inspected at that revision. The release currently provides source only. The owner has now requested bilingual README preparation, agent-to-agent icon handoff and publication of Hexly v0.5.2.

The bilingual entry lives at `/logos/showtime`. In the default directory order it precedes InfoSpace at the start of the tools series. In the curated Tools filter it is first; the relative order of other projects and alphabetical sorting are preserved. The profile generator assigns the next available numbered profile without renaming earlier files.

The matching GitHub profile addition was published at `73203ef18b98ccf7cf03be7ef0ebf327bb8a743d`; its original local capture remains in the source snapshot and [profile text backup](sources/nocoo-readme-2026-09-10-showtime.md). The repository description already matches the profile and site. The storefront does not invent a product website. Chinese `README.md` and English `docs/README.en.md` were committed separately at `9039ab8`, based on the actual features, CLI/MCP interfaces and workflow conventions. They describe source builds, with no claim of downloadable application binaries or audio export.

## Classic clapperboard with green presentation

The owner's direction is a high-quality 3D director's clapperboard. Study 01 placed the requested green on the board itself; the owner rejected that direction and requested a classic black-and-white board with lettering. Green now belongs to the later background. The `#61BB46` design anchor, interpreted from the top green stripe of the classic rainbow Apple logo, appears in the independent presentation motif. The board belongs to the material series; animal fragmentation rules do not apply.

The source UI is being independently updated during this task. Its recorded accent is `#4A8234`, surface `#F6F8F4`, ink `#253126` and pale green `#CFE8B5`. These source tokens remain separate from the artwork design anchor and eventual native pixel samples. Showtime's Codex owns the application changes and actual icon consumers.

Two Azure `gpt-image-2` requests each returned a native 2048 × 2048 PNG. The rejected green board and exact feedback remain in study 01. Study 02 preserves the revised [brief](../artwork/logo-family/showtime/2026-09-10-02/brief.md), [exact prompt](../artwork/logo-family/showtime/2026-09-10-02/prompt.txt), ordered references, sanitized request/response and [untouched black-and-white image](../artwork/logo-family/showtime/2026-09-10-02/raw/generated.png). The model route uses workflow's existing `azure-gpt-image-cover` credentials through direnv.

The owner approved the exact raw bytes with “审查通过，继续”, recorded in [raw-review.json](../artwork/logo-family/showtime/2026-09-10-02/raw-review.json). Finishing **03** supplies the selected transparent foreground and pale sage presentation, with green film-registration brackets, perforation marks and editing cues. The native board remains charcoal, ivory and silver; SHOWTIME, ROLL, SCENE, TAKE and the three 01 fields remain intact.

## Finishing and source coordination

The [finishing inspection](../artwork/logo-family/showtime/2026-09-10-02/inspection/finishing-03.json) verifies all ten export sizes, no alpha holes in the slate interior, 178.5 px minimum rounded clearance and zero clipped pixels. All 2,149,115 fully opaque native pixels preserve their original RGB. At 32/24/16 px, the silhouette and striped clapstick carry recognition; small field text is no longer legible. Superseded passes 01 and 02 remain archived with their residue/fringe findings.

The [static study](../artwork/logo-family/showtime/2026-09-10-02/review.html) and `/logos/showtime` supply the complete before/after comparison, Icon/Transparent/White modes, palette, small marks, contrasting edge checks, downloads and exact prompt. Presentation reference boards remain in the static study and Git archive only.

The [handoff](../artwork/logo-family/showtime/2026-09-10-02/handoff.json) supplies exact masters and hashes to Showtime's Codex in Herd, pane w2H:p1. It copied root `logo.png` and `logo-readme.png` byte-for-byte; both hashes were independently verified. This agent committed only README text in Showtime. Application consumers and the independent icon commit belong to Showtime Codex, which decides its own app release. Hexly's authorized release target is the explicit patch increment **0.5.2**, using the [release runbook](05-release.md).

## Verification

The preparation passes TypeScript, Biome, isolation and security gates, all 94 unit tests, all 62 HTTP tests and 28 affected desktop/mobile browser tests. Source snapshots, 71 original checksums, 426 display derivatives, 55 historical public archives and 93 finishing manifests verify successfully. The production build succeeds. Existing source identities, generated profiles and artwork remain unchanged outside the Showtime addition and directory index.

Those preparation checks covered the historical baseline and pending-source behavior. The completed family archive separately passes verification of 71 source checksums, 426 display derivatives, 56 current/historical public archives and 95 finishing passes. Both README JSON examples parse, documented CLI invocations and eight MCP tool names match implementation, and all 22 local links resolve. Local browser review uses `https://index.dev.hexly.ai/logos/showtime` and the static study; release completion requires the exact commit's successful Deploy job and production verification.

The [browser inspection](../artwork/logo-family/showtime/2026-09-10-02/inspection/browser/checks.json) records desktop/mobile checks of both complete pages, all three presentation modes, light/dark themes, English/Chinese catalogue text, image decoding, palette clipboard actions, prompt equality and an exact-hash transparent download. Eight screenshots preserve the rendered pages. Both layouts have no horizontal overflow or page errors; the artwork and small marks were visually inspected.
