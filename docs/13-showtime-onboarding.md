# Showtime onboarding and icon preparation

Showtime turns scripted interactions with real webpages into macOS product demo movies. SwiftUI and AppKit provide the studio, WebKit hosts the pages, AVFoundation exports MP4, and Python exposes CLI and MCP control. Current exports have no audio track.

## Local storefront

The owner requested a preparatory entry before the project's first push. At that intake, the public `nocoo/showtime` repository was verified as empty. The inspected source files are copied into the [study inputs](../artwork/logo-family/showtime/2026-09-10-01/input/catalogue/), indexed by the [checksummed source snapshot](sources/showtime-2026-09-10.json). That historical snapshot records the actual theme tokens and each file's SHA-256. Its null revision states that there was no inspected commit at capture; the historical evidence remains intact.

The source project subsequently published `1fa4de8686166d4477ab78c7f3356aa8e19b403d` and v1.0.0. The [second study's source capture](../artwork/logo-family/showtime/2026-09-10-02/sources.json) records files inspected at that revision. The release currently provides source only. The owner has now requested bilingual README preparation, agent-to-agent icon handoff and publication of Hexly v0.5.2.

The bilingual entry lives at `/logos/showtime`. In the default directory order it precedes InfoSpace at the start of the tools series. In the curated Tools filter it is first; the relative order of other projects and alphabetical sorting are preserved. The profile generator assigns the next available numbered profile without renaming earlier files.

The matching GitHub profile addition was published at `73203ef18b98ccf7cf03be7ef0ebf327bb8a743d`; its original local capture remains in the source snapshot and [profile text backup](sources/nocoo-readme-2026-09-10-showtime.md). The repository description already matches the profile and site. The storefront does not invent a product website. Chinese `README.md` and English `docs/README.en.md` are prepared from the actual features, CLI/MCP interfaces and workflow conventions.

## Classic clapperboard with green presentation

The owner's direction is a high-quality 3D director's clapperboard. Study 01 placed the requested green on the board itself; the owner rejected that direction and requested a classic black-and-white board with lettering. Green now belongs to the later background. The original `#61BB46` design anchor, interpreted from the top green stripe of the classic rainbow Apple logo, remains a proposed presentation reference. The board belongs to the material series; animal fragmentation rules do not apply.

The source UI is being independently updated during this task. Its recorded accent is `#4A8234`, surface `#F6F8F4`, ink `#253126` and pale green `#CFE8B5`. These source tokens remain separate from the artwork design anchor and eventual native pixel samples. Showtime's Codex owns the application changes and actual icon consumers.

Two Azure `gpt-image-2` requests each returned a native 2048 × 2048 PNG. The rejected green board and exact feedback remain in study 01. Study 02 preserves the revised [brief](../artwork/logo-family/showtime/2026-09-10-02/brief.md), [exact prompt](../artwork/logo-family/showtime/2026-09-10-02/prompt.txt), ordered references, sanitized request/response and [untouched black-and-white image](../artwork/logo-family/showtime/2026-09-10-02/raw/generated.png). The model route uses workflow's existing `azure-gpt-image-cover` credentials through direnv.

The new raw-image decision is pending in [raw-review.json](../artwork/logo-family/showtime/2026-09-10-02/raw-review.json). The untouched lettered clapperboard has been shown to the owner. No extraction, compositing, derivative export or integration of these new bytes occurs before that decision. The current storefront therefore preserves the initial application artwork as its source baseline.

## Next authorized steps after the image decision

After selection, finish the independent transparent foreground and sage film-registration presentation, inspect the rounded outline and all ten export sizes, and produce the individual static review and complete catalogue comparison. Deliver the exact masters and hashes to Showtime's Codex in Herd, pane w2H:p1. It will install root `logo.png` and `logo-readme.png` and update the actual macOS consumers; its own publication decision is separate from the website release. Keep the model's raw response immutable.

Record the commits containing the selected source images and completed READMEs, verify the localized links, synchronize the catalogue/profile and publish Hexly with the explicitly requested patch increment to 0.5.2. This agent changes README text in Showtime; application code and app icon adoption belong to its existing Codex.

## Verification

The preparation passes TypeScript, Biome, isolation and security gates, all 94 unit tests, all 62 HTTP tests and 28 affected desktop/mobile browser tests. Source snapshots, 71 original checksums, 426 display derivatives, 55 historical public archives and 93 finishing manifests verify successfully. The production build succeeds. Existing source identities, generated profiles and artwork remain unchanged outside the Showtime addition and directory index.

The local Vite server serves the entry through `https://index.dev.hexly.ai/logos/showtime`. These checks cover the preserved baseline entry and pending-source link behavior. They do not represent approval or finishing of the new clapperboard image, and no deployment has occurred.
