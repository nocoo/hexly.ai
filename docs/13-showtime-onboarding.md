# Showtime onboarding and icon preparation

Showtime turns scripted interactions with real webpages into macOS product demo movies. SwiftUI and AppKit provide the studio, WebKit hosts the pages, AVFoundation exports MP4, and Python exposes CLI and MCP control. Current exports have no audio track.

## Local storefront

The owner requested a preparatory entry before the project is pushed. The public `nocoo/showtime` repository exists and was verified as empty. The inspected source files are copied into the [study inputs](../artwork/logo-family/showtime/2026-09-10-01/input/catalogue/), indexed by the [checksummed source snapshot](sources/showtime-2026-09-10.json). The snapshot records the actual theme tokens and each file's SHA-256. A null revision states that there was no inspected commit; it is not replaced with a fabricated revision.

The bilingual entry lives at `/logos/showtime`. In the default directory order it precedes InfoSpace at the start of the tools series. In the curated Tools filter it is first; the relative order of other projects and alphabetical sorting are preserved. The profile generator assigns the next available numbered profile without renaming earlier files.

The matching GitHub profile addition is a local commit, recorded in the source snapshot and [profile text backup](sources/nocoo-readme-2026-09-10-showtime.md). Source publication, remote repository-description synchronization and Showtime README editing are deferred. The storefront does not invent a website or link to an unavailable localized README.

## Green physical-object identity

The owner's direction is a high-quality 3D director's clapperboard. The design anchor is `#61BB46`, interpreted from the top green stripe of the classic rainbow Apple logo. The fresh green enamel body, charcoal and ivory clapsticks, and brushed nickel hinge belong to the material series. Animal fragmentation rules do not apply.

The existing source UI was independently updated during this task. Its recorded accent is `#4A8234`, surface `#F6F8F4`, ink `#253126` and pale green `#CFE8B5`. These source tokens remain separate from the artwork design anchor and eventual native pixel samples. The icon task preserves the concurrent application changes and the staged initial source tree.

One Azure `gpt-image-2` request returned a native 2048 × 2048 PNG. The [brief](../artwork/logo-family/showtime/2026-09-10-01/brief.md), [exact prompt](../artwork/logo-family/showtime/2026-09-10-01/prompt.txt), ordered references, sanitized request/response and [untouched image](../artwork/logo-family/showtime/2026-09-10-01/raw/generated.png) are archived. The model route uses workflow's existing `azure-gpt-image-cover` credentials through direnv.

The raw-image decision is pending in [raw-review.json](../artwork/logo-family/showtime/2026-09-10-01/raw-review.json). The untouched clapperboard has been shown to the owner. No extraction, compositing, derivative export or integration of this new image occurs before its exact-byte decision. The current storefront therefore preserves the initial application artwork as its source baseline.

## Next authorized steps after the image decision

After selection, finish the independent transparent foreground and sage film-registration presentation, inspect the rounded outline and all ten export sizes, and produce the individual static review and complete catalogue comparison. Adopt the selected source assets locally using the native macOS masking contract and preserve their exact hashes. Keep the model's raw response immutable.

When the owner resumes repository and README work, replace pending source provenance with the commit containing those exact assets. Apply the workflow README conventions, add verified localized README links and synchronize the remote repository description and profile when publication is authorized.

## Verification

The preparation passes TypeScript, Biome, isolation and security gates, all 94 unit tests, all 62 HTTP tests and 28 affected desktop/mobile browser tests. Source snapshots, 71 original checksums, 426 display derivatives, 55 historical public archives and 93 finishing manifests verify successfully. The production build succeeds. Existing source identities, generated profiles and artwork remain unchanged outside the Showtime addition and directory index.

The local Vite server serves the entry through `https://index.dev.hexly.ai/logos/showtime`. These checks cover the preserved baseline entry and pending-source link behavior. They do not represent approval or finishing of the new clapperboard image, and no deployment has occurred.
