# Eagle onboarding — 2026-09-19

Eagle is a private, evidence-led view of Herdr Spaces across reporting machines.
The source uses React 19, Vite, Basalt, a Cloudflare Worker, D1 and local Node
collectors. Task evidence, capture order, stale data and history remain explicit;
status labels do not certify completed work. No private machine inventory is
copied into the public catalogue.

The owner requested a new animal-series eagle and full onboarding without an
interactive confirmation. The exact instruction is preserved before generation
in `artwork/logo-family/eagle/2026-09-19-01/brief.md`. The native 2048px Sunburst
output is kept unchanged; `raw-review.json` records agent inspection under
`owner-delegated batch acceptance`, with `ownerReviewedExactBytes: false`.
The old generic Lucide Bird UI geometry and license are recorded separately;
there was no earlier independent project identity.

The golden eagle's connected flat facets cover its crown, cheek, neck and wing
roots. One multicolored wind streamer extends from the hooked beak. Authored
mountain-pine needles and sparse alpine leaves provide a separate habitat motif
on warm limestone paper. Basalt site colors remain independent.

Finishing 02 retains the native extraction and uniformly places the group at
0.9 scale. A one-pixel baseline correction preserves the natural bottom shoulder
entry. All measured head/streamer pixels clear the rounded boundary by at least
186.5px, with no feature clipping. Intended lower shoulder intersections are
recorded separately. Masters and all ten sizes, alpha masks, shadows, settings,
tool snapshots, previous pass, sampled palette and hashes are archived.

The full brand kit 1.0.0 contains exact original artwork, transparent small marks,
light/dark presentations, a seven-size ICO, licensed wordmark outlines, lockups,
independently composed wide/mobile Heroes, both botanical texture variants,
usage, provenance and rights. Assembly makes no additional image-generation
call. Canonical archive: `/projects/eagle#brand`; standalone kit specimens:
`/brands/eagle/v1.0.0/review.html`; workbench comparison is retained locally.

Both source README languages use the rounded presentation. Root `logo.png`
preserves the selected transparent master. Expanded/collapsed sidebar and other
application identity surfaces use transparent marks with no tile or extra mask.
GitHub → Hexly → Theme controls reuse Basalt and compact accessible tooltips.
The Apple touch icon supplies a square presentation for platform masking.

`GET /api/live` already checked D1 but omitted the application version. A real
Miniflare/D1 test reproduced that omission before the endpoint was corrected to
read root `package.json`. No-store, dependency failure and bounded public
metadata remain intact; business authentication is preserved. Status coverage
is derived from the public website and begins with real Cron observations only.

Profile commit: `4e5549022a13319e84de03ad4d66863af9188264`.
Source identity adoption: `d03eb4d276ec20ead665e490eb78ce5b09d720a5`.
Source snapshot and palette evidence: `docs/sources/eagle-2026-09-19.json`.
The catalogue appends Eagle without renumbering older profiles; animal ordering
records actual source popularity at intake. Frozen unrelated catalogue entries
and all historical immutable assets are preserved.

Zeppelin's follow-up is published separately as v1.0.2, commit
`560f5486b751b1345ac69cdc737436165a823645`. Its README retains the spacecraft Logo,
while site chrome uses only the text wordmark as requested. CI/deployment and
all 7 unit plus 26 browser/HTTP checks passed; `/api/live` identifies that release.

Eagle's source authentication redesign and Zeppelin's vessel-preview work were
active in other checkouts. This batch uses isolated publication checkouts and
preserves those changes. Final source release, catalogue commit, deployment and
observed Status results are recorded in the release completion receipt.

Eagle v0.1.1 was deployed from `42d0ddd0c2ee49c3f59e7107ee84736d3cdc36b5`
with successful source CI `35429841183`, 33 unit/HTTP checks and 12 desktop/mobile
browser cases. Real local Herdr ingestion, D1 inventory, refresh and responsive
UI verification passed. Production public health reports v0.1.1 with no-store;
the business homepage redirects anonymous visitors to Cloudflare Access. The
authenticated production UI was not exercised without an Access session.
Private local evidence and screenshots are not included in these archives.
Details: [source publication](../artwork/logo-family/eagle/2026-09-19-01/source-publication.json)
and [archive browser verification](../artwork/logo-family/eagle/2026-09-19-01/browser-verification.json).

R2 publication verified 165 Eagle objects plus the preserved first-adoption
backup alias, with immutable complete-byte and MIME receipts. Hexly validation
passed 430 unit checks with coverage above 98% in all four dimensions. HTTP,
focused Eagle browser checks and final release CI are recorded with deployment
in the release completion receipt.
