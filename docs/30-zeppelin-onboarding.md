# Zeppelin onboarding — 2026-09-19

Zeppelin is an original industrial science-fiction spacecraft fleet and design
archive: 17 mission series, 25 entries and three modeled vessels with seven
preserved HD views each. Its catalogue category is Design. Planned ships retain
explicit placeholders and unknown dimensions; the new symbolic Logo does not
add a fictional fleet model.

The owner authorized complete onboarding and Hexly Z+1 publication without an
interactive review. The named-project waiver, exact prompt, native Sunburst
output and agent acceptance are preserved in
[study 2026-09-19-01](../artwork/logo-family/zeppelin/2026-09-19-01/brief.md).
`ownerReviewedExactBytes` is false. The former yellow Z favicon is preserved,
and was excluded from generation references.

The adopted identity uses a titanium hull, graphite joints, yellow enamel bands
and twin amber engine cores. Its material series follows the explicit 3D owner
direction. Uniform 0.8 placement provides 228.5px minimum rounded clearance,
with zero clipped pixels; all 1,424,341 fully opaque native pixels retain their
original RGB values. Ten export sizes span 2048px to 16px; the transparent ICO
contains seven independently decoded sizes. Fine seams soften at small sizes.

Separate cool engineering-paper surfaces use approach chevrons, broken range
arcs and docking alignment ticks. Brand kit 1.0.0 includes the original identity,
transparent marks, licensed Space Grotesk outlines, lockups, independently
composed wide/mobile Heroes, both themes, authored motif textures, provenance,
usage and rights. All generation and finishing history remains available.
The product keeps its actual #101110 / #edeee8 / #dddf45 site tokens.

| Publication | Record |
| --- | --- |
| Source site | https://zeppelin.hexly.ai |
| Source release | [v1.0.1](https://github.com/nocoo/zeppelin/releases/tag/v1.0.1) |
| Source adoption | `8ae8e920dd3afc9114c6e9d917f698e54dc95157` |
| Source CI / deployment | `35420317302` / `35420377569`, both successful |
| Profile commit | `807c5028ce7b5c085a875ce2f9714275549f2e3e` |
| Catalogue / brand | `/projects/zeppelin`, `/projects/zeppelin#brand` |
| Brand package | `/brands/zeppelin/v1.0.0/` |
| Health endpoint | https://zeppelin.hexly.ai/api/live |
| Hexly patch target | v0.15.3, from v0.15.2 |

The source's existing material policy excludes high-resolution binaries from
Git. Its committed `public/brand.json` pins immutable R2 bytes and hashes; the
exact root `logo.png` and separate presentation masters are hydrated locally.
Catalogue `sourceUrl` identifies the actual CDN master, while the repository
revision identifies that adoption receipt. No nonexistent Git image blob is
claimed, and the source material guard is unchanged.

The homepage uses the transparent mark, real version, GitHub → Hexly controls,
localized labels and keyboard-visible tooltips. The current full-screen vessel
inspector deliberately has no site header. Its layout and the parallel source
3D-preview work are preserved. README uses the rounded presentation; browser
and small UI marks use transparent artwork with no additional mask.

Status derives Zeppelin automatically from the verified independent HTTPS
website. Its unauthenticated health response reports `status: "ok"`, version
1.0.1 and the deployed source revision, with `Cache-Control: no-store`.
No D1 target row, synthetic observation or backfill is required. The first
production observation follows Hexly deployment and the next five-minute Cron.

The catalogue is appended as project 77, preserving historical order and profile
numbers. Source/profile descriptions match. R2 inventory and full-byte receipts
cover only Zeppelin additions; other projects' frozen materials are preserved.
Source validation passed seven unit checks and 26 browser/HTTP cases. Hexly
validation includes 429 unit checks, 141 HTTP checks, complete source/finishing
hash checks and browser journeys. Two historical snapshot tests were adjusted
to account for the earlier, already-published Ellie/Life website onboarding;
all other frozen metadata remains protected.

Detailed evidence: [source snapshot](sources/zeppelin-2026-09-19.json),
[source publication](../artwork/logo-family/zeppelin/2026-09-19-01/source-publication.json),
[artwork verification](../artwork/logo-family/zeppelin/2026-09-19-01/verification.json),
and [R2 receipts](assets/publication.jsonl). Final deployment and observed Status
results are recorded in the release completion receipt.
