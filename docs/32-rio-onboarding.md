# Rio onboarding — 2026-09-20

Rio is a browser voice interface for local Hermes agents, powered by GPT-Live
and Cloudflare. Its public product copy describes the complete intended
experience, as explicitly requested by the owner: natural voice conversations,
local reasoning and tools, and switching between machines, gateways and agent
profiles. The source evidence snapshot separately records what was inspected.

The owner selected the bird series, a handsome colorful macaw, connected flat
facets and an off-center composition. The instruction waived intermediate image
confirmation for Rio and prioritized copying the Logo to the source root.
One native 2048 × 2048 `gpt-image-2.5-sunburst` image establishes the first
independent identity. The exact prompt, ordered references, sanitized request,
response and untouched PNG live in `artwork/logo-family/rio/2026-09-20-01/`.
`raw-review.json` records agent acceptance against its exact hash and explicitly
sets `ownerReviewedExactBytes: false`.

Finishing 01 preserves 2,414,265 fully opaque native pixels with zero RGB
changes. Border-connected extraction removes the sampled near-white exterior
while retaining the ivory cheek, beak and eye highlight. The natural shoulder
crosses the lower-left and bottom edges; the complete face and floating feather
clear the final rounded outline. Separate palm/heliconia relief, warm coral
paper, grain and shadows evoke the scarlet macaw's rainforest habitat. Native
palette coordinates, protected-feature measurements, all ten export sizes and
frozen processing layers are archived.

The transparent master is committed at Rio root `logo.png`, with square and
rounded presentation masters under `assets/brand/`, in source adoption commit
`f4edf15`. The source application is under concurrent development. This task's
source handoff changes those new identity files only; the implementation owner
retains the current wordmark, glass illustration, browser icon and headers.
Use transparent marks for future UI/favicon consumers and retain the separate
presentation roles. The source header's eventual family link is
`https://hexly.ai/projects/rio`, following the shared site-entry contract.

The complete 1.0.0 campaign kit reuses this foreground byte for byte. It adds
licensed Space Grotesk outlines, light/dark lockups, an ICO with transparent
entries, independently composed wide/mobile Heroes, habitat textures, usage,
rights and checksums. Assembly makes no additional image-model call. The kit
uses Hexly campaign colors; Rio's own forest-green, warm-paper and ink tokens
remain separately evidenced and unchanged.

Profile commit `59ee4e3` is confirmed on the remote default branch. GitHub's repository description now uses the
same parrot emoji and English description as the profile and catalogue. Rio is
appended to the catalogue so existing numbered profiles remain stable. The
original source backup and every other project's historical identity remain
unchanged. Evidence: `docs/sources/rio-2026-09-20.json`.

The source production endpoint `https://rio.hexly.ai/api/live` returned HTTP 200
JSON, `status: ok`, version `0.1.0` and `Cache-Control: no-store` without a
credential or redirect. Its returned revision is a Worker deployment ID, not a
Git SHA. The inspected endpoint establishes liveness only and does not probe
D1. Status coverage derives from the catalogue and needs no separate D1 row.
Real production observations start after the Hexly release's next Cron; local
dev observations remain explicitly marked demonstrations.

The owner authorized Hexly v1.0.0, reviewed the local dev preview, and explicitly
accepted publication with “没毛病，继续发布。” on 2026-09-20. Root `package.json`
is prepared at 1.0.0. Publish the scoped R2 materials and use the normal release
workflow; verify its exact CI/Deploy revision and Rio's first actual production
Status result. This preview acceptance does not rewrite the earlier delegated
raw-image review record. The source Logo commit remains local while its owner
continues development; do not publish concurrent source work as part of this
Hexly release.

Local preview: `https://index.dev.hexly.ai/projects/rio`. The legacy
`/logos/rio` route points to the same complete `#brand` archive. Standalone kit:
`/brands/rio/v1.0.0/review.html`; generation workbench:
`/artwork/logo-family/rio/2026-09-20-01/review.html`.
